"""Phase 4 — representation extraction.

Model-agnostic helpers (`common_fields.py`, `cache.py`) shared by every
`experiments/extract_<model>.py` script. See
`paper/phase4_representation_extraction.md` for the full schema/design
write-up.
"""
