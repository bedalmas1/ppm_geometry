# Project Status

> Living tracker. Update this file at the end of every session — new sessions should read this file first, then jump to the relevant phase in [`PLAN.md`](./PLAN.md). Keep entries dated; never delete history from the decision log, only append.

**Current phase:** Phase 4 — Representation extraction — COMPLETE (all 9 models' Helpdesk test-set embeddings extracted and cross-validated against Phase 3's recorded metrics). Next: Phase 5 — Geometry metric implementation & synthetic validation (must be validated on synthetic trajectories before touching these real caches — see PLAN.md's pilot-first rule).
**Last updated:** 2026-08-17

---

## Phase checklist (mirrors PLAN.md — tick here as work completes)

- [x] Phase 0 — Repository & tooling scaffolding
- [x] Phase 1 — Literature & repository audit
- [x] Phase 2 — Dataset pipeline
- [x] Phase 3 — Baseline reproduction
- [x] Phase 4 — Representation extraction
- [ ] Phase 5 — Geometry metric implementation & synthetic validation
- [ ] Phase 6 — Descriptive geometry study (pilot → full scale)
- [ ] Phase 7 — Reliability study
- [ ] Phase 8 — Future-organization study
- [ ] Phase 9 — Explainability / event-importance study
- [ ] Phase 10 — Controls, ablations & statistical synthesis
- [ ] Phase 11 — Paper

---

## Current phase detail

### Phase 0 — Repository & tooling scaffolding — DONE
- Dependency manager: `uv` (installed via `pip install --user uv`; not on this machine's PATH inside the bash tool, so `python -m uv <cmd>` was used — works identically; a normal shell with `uv`'s install location on PATH will have `uv` directly).
- `pyproject.toml`: project metadata, `requires-python = ">=3.11,<3.14"` (permissive placeholder — Phase 1 may need to narrow this if a chosen baseline repo requires an older torch/Python combination), `[tool.uv] package = false` (this is a research repo, not a distributable package — imports rely on `pythonpath = ["src"]` in pytest config, no editable install needed), `dev` dependency group with `pytest`.
- `uv.lock` generated and committed.
- `.venv/` created locally (gitignored).
- `.gitignore` added: excludes `.venv/`, caches, `data/raw/*` and `data/processed/*` (keeps `.gitkeep`), `results/*` and `figures/*` (keeps `.gitkeep`), checkpoint file extensions, notebook checkpoints, editor/OS cruft. Explicitly keeps `uv.lock`, `PLAN.md`, `STATUS.md`.
- `tests/test_scaffold.py` placeholder + `pytest` config (`testpaths`, `pythonpath`) — `uv run pytest -q` passes (1 test). Removed `tests/.gitkeep` since a real file now lives there.
- `configs/README.md` documents the config convention (one YAML per experiment; `datasets/`, `models/`, `experiments/` subdirectories to be created as needed) — replaced `configs/.gitkeep`.
- `README.md` written: project summary, setup instructions, full repo layout, pointers to STATUS/PLAN/CLAUDE.

Not yet decided in Phase 0 (deferred, not blocking): experiment tracking/logging format (lightweight structured logs vs. external tracker) — will decide once Phase 3 (baseline reproduction) actually needs to log training runs.

### Phase 1 — Literature & repository audit — DONE (three rounds: initial → user-directed strengthening → systematic review)
Full write-up: [`paper/related_work_model_audit.md`](./paper/related_work_model_audit.md) — this is now the authoritative source for all model-selection reasoning; this section is a summary only.

**Round 1** (initial ad-hoc pass): 6-model roster (2 Family A + 1+1 Family B, no outcome). **Round 2** (user pushback: single model per Family A category too thin for reviewers): added Camargo's GenerativeLSTM as 2nd next-event model, resolved CRTP-LSTM's peer-review provenance (IEEE TSC 2023) as 2nd suffix model, dropped outcome prediction entirely (not just from Family A). **Round 3** (user flagged a real gap — GNN-based next-event models had never been searched for — and asked for a *systematic* review rather than sequential ad-hoc passes, with a documented methodology): ran dedicated systematic searches (2021–2026) for next-event, GNN-specifically, and full-suffix, then hands-on cloned/verified every new candidate. User explicitly relaxed the adoption bar for this round to "peer-reviewed or arXiv preprint + usable repo," and redefined the goal as "3–4 architecturally distinct models per category to experiment with, decide paper inclusion later."

**Key finding from the systematic review:** next-event and full-suffix are *not symmetric* in the literature — next-event PPM research (2021–2026) is high-publication/low-code (of ~17 papers found across both the GNN-specific and general passes, only 2 had any repo, and one of those turned out not to deliver what it claimed on hands-on inspection), while full-suffix is comparatively rich in reproducible work (4 models cleared the bar: SuTraN, CRTP-LSTM, LUPIN, MLMME, spanning 4 distinct architectural paradigms).

**Final roster: 9 configurations** (well beyond the spec's stated 3–6, by explicit user direction):

- Family A next-event (3): **A1** ProcessTransformer (Transformer, TF/Keras, Apache-2.0, arXiv-only provenance disclosed) · **A2** Camargo GenerativeLSTM (stacked LSTM, TF/Keras, Apache-2.0, BPM 2019) · **A3** RLHGNN (heterogeneous GNN + RL, PyTorch+DGL, arXiv 2507.02690 not peer-reviewed, **no LICENSE file at all — disclosed risk**, the only candidate found across two searches that delivers genuine GNN architectural diversity with working single-case code).
- Family A full-suffix (4): **A4** SuTraN (Transformer enc-dec, PyTorch, MIT, ICPM 2024) · **A5** CRTP-LSTM (direct/non-autoregressive LSTM, PyTorch, MIT, IEEE TSC 2023) · **A6** LUPIN (BERT/LLM fine-tuning, PyTorch+HF transformers, **CC BY-NC-SA 4.0** — isolate its code, don't merge into permissively-licensed modules, ICPM 2024, fully version-pinned `requirements.txt` — best reproducibility hygiene of any model audited) · **A7** MLMME (Taymouri & La Rosa, adversarial/GAN-trained RNN enc-dec, PyTorch, **GPL-3.0** — isolate its code, SDM 2021).
- Family B (2, unchanged): **B1** Transformer-next, **B2** Transformer-suffix, built in-house Phase 3. No outcome objective.

**A notable hands-on correction**: TGN-AST (Hennig & Schmidt, BPM 2025 main track — stronger peer review than ProcessTransformer, MIT-licensed) was initially considered as the GNN-diversity pick, but reading its actual source revealed it is a **TensorFlow/Keras Transformer variant**, not an end-to-end GNN — the graph component is a decoupled pretraining step producing an input feature. Rejected: doesn't deliver the architectural diversity it was sought for, and is Colab-oriented/notebook-only with a 642MB repo. RLHGNN took its place instead.

**Licensing summary across the full roster:** Apache-2.0 (A1, A2) · MIT (A4, A5) · CC BY-NC-SA 4.0, isolate (A6) · GPL-3.0, isolate (A7) · **no license at all, disclosed risk** (A3). No license blocks academic research use, but A6/A7's code must stay isolated/clearly attributed (same handling as SPICE's ND license got), and A3's gap must be stated explicitly in the paper — consider emailing RLHGNN's authors for clarification.

**Hands-on clone verification completed for:** ProcessTransformer, SuTraN, CRTP-LSTM (architecture confirmed, exact z_t layer still TBD), Camargo GenerativeLSTM, LUPIN, MLMME, RLHGNN, TGN-AST (rejected after inspection). All scratch-only clones, nothing added to this repo.

**New findings requiring Phase 3 follow-up:** ProcessTransformer's `setup.py` unpinned beyond `tensorflow>=2.4`; Camargo's `environment.yml` pulls an unpinned git dependency over **plain HTTP** (`git+http://github.com/Mcamargo85/support_modules.git`) — fix in Phase 3; SuTraN/CRTP-LSTM/RLHGNN have no dependency files at all, need pinning ourselves.

**Framework groups needed (feeds Phase 2's `pyproject.toml`, already added):** `tf` (A1, A2) · `torch` (A4, A5, A7, B1, B2) · `torch-hf` (A6, adds HuggingFace `transformers`) · `torch-dgl` (A3, adds DGL, isolated since DGL has its own torch/CUDA version-matching constraints).

**Rejected/deferred, logged not silently dropped:** I3SP (independent-lab suffix model, fallback if a "SuTraN/CRTP-LSTM same lab" critique arises), TGN-AST, Wang & Damiani's Time-Aware GNN (has code, not pursued once RLHGNN was adopted), SuTraN+, CoLES/`pytorch-lifestream`, Tax et al.'s original LSTM repo, every no-repo paper found in the systematic searches (documented as a "code doesn't exist yet" gap, candidate for a journal-extension follow-up — chase authors or reimplement), no outcome-prediction model (dropped entirely, H3 explicitly untested).

**ICPM 2027 / ML4PM CFP:** conference dates confirmed (Feb 8–12, 2027, University of Calabria); no ML4PM-specific CFP, deadline, or page limit found yet — too early. Re-check before Phase 11.

### Phase 2 — Dataset pipeline — DONE
Full write-up: [`paper/dataset_selection.md`](./paper/dataset_selection.md) — real, computed statistics for all 5 datasets, not literature figures.

**Dataset roster (finalized, downloaded, processed):** BPIC12, BPIC17, BPIC19, Sepsis, Helpdesk — all 5 downloaded (`data/raw/`, gitignored) from verified 4TU.ResearchData direct links (exact byte-size match confirmed for every file, nothing guessed) and run through the pipeline into `data/processed/<dataset>/{train,val,test}.parquet` + `manifest.json`. Every case/event/activity count matches the literature exactly (e.g. BPIC12: 13,087 cases/262,200 events; BPIC19: 251,734 cases/1,595,923 events/42 activities). **Data-quality finding, not yet resolved:** BPIC19's computed temporal span starts 1948-01-26 — a known data-quality artifact (placeholder timestamps on a handful of events), not a real 72-year process or a pipeline bug; needs an explicit cleaning rule before Phase 3 training.

**Pipeline implementation:** `src/data/` is now a proper Python package (`schema.py` canonical case_id/activity/timestamp columns, `loaders.py` — XES via `pm4py`, CSV for Helpdesk, both converging to the same schema, `splits.py` — time-based case-sort split with a sha256 provenance hash, `stats.py` — full spec §6 descriptive-stats list). Driven by `scripts/prepare_dataset.py` (config-driven, one YAML per dataset in `configs/datasets/`, `--all` flag to run everything). 18 unit tests (`tests/test_loaders.py`, `test_splits.py`, `test_stats.py`) validate the splitting/stats logic against synthetic data with hand-computed expected values — pass without needing the real (gitignored) datasets. Caught and fixed a real Windows-only bug along the way: `Path.read_text()`/`write_text()` without `encoding="utf-8"` silently mojibake's non-ASCII characters (em dashes, §) on this locale — fixed in `scripts/prepare_dataset.py`.

**Major environment finding — Python 3.13 → 3.12:** the dependency-group smoke test (see below) discovered TensorFlow 2.19.1 ships no wheels for CPython 3.13 (cp311/cp312 only). Since A1/A2 need TensorFlow, **the whole project was switched from Python 3.13.0 to 3.12.10** (`.python-version`, `pyproject.toml`'s `requires-python` now `>=3.11,<3.13`) — every other framework in the roster supports 3.12 fine, so one shared Python version was kept rather than fragmenting per framework.

**Dependency-group smoke test — all 4 groups (`tf`, `torch`, `torch-hf`, `torch-dgl`) now genuinely verified working** (installed AND actually imported AND ran a basic operation, not just "pip install succeeded"). This surfaced and fixed a chain of real, non-obvious issues, in order:
1. TF/CPython 3.13 incompatibility (above) → switched to Python 3.12.
2. `torch-dgl`: dgl==2.2.1's PyPI wheel bundles a **precompiled C++ `graphbolt` extension for specific torch builds only** (inspected the wheel directly: 2.1.0–2.1.2, 2.2.0–2.2.2, 2.3.0 — nothing newer). The unpinned `torch>=2.3` constraint resolved to whatever the latest available torch was and failed at import with a missing-DLL error. **Fixed: pinned `torch==2.3.0`, `dgl==2.2.1` exactly.**
3. dgl's `graphbolt` module imports `torchdata.datapipes`, an API **removed** in newer torchdata releases (confirmed: torchdata 0.11 raises `ModuleNotFoundError`). **Fixed: pinned `torchdata==0.7.1`**, the last release still shipping it.
4. dgl's compiled extensions were built against NumPy 1.x; NumPy 2.x triggers ABI warnings. **Fixed: `numpy<2` added to the `torch-dgl` extra.**
5. dgl's `graphbolt.impl.ondisk_metadata` imports `pydantic` at runtime but doesn't declare it as a dependency — a packaging bug in dgl itself. **Fixed: `pydantic` added explicitly.**
6. **A structural fix, not just a pin:** uv resolves ONE universal lockfile satisfying every optional-dependency group simultaneously by default. `torch-dgl`'s necessary `torch==2.3.0` pin was silently bleeding into `torch-hf`, breaking `transformers` (which self-disables its PyTorch backend under torch<2.5 — a runtime check, not a declared packaging constraint, so uv had no visibility into the conflict). **Fixed properly: added `[tool.uv] conflicts` declaring `tf`/`torch`/`torch-hf`/`torch-dgl` mutually exclusive**, so uv resolves an independent version set per extra instead of one shared set — matches reality, since no training run ever needs two of these frameworks at once.
7. Even with conflicts declared, `torch-hf` still needed an explicit `torch>=2.5` floor added to its own extra (the resolver has no data-level reason to pick a newer torch than the declared minimum, since `transformers`' real requirement is invisible to it).

Final state: `tf` → TensorFlow 2.19.1 (Python 3.12, standalone). `torch` → torch 2.13.0 (current, for A4/A5/A7/B1/B2). `torch-hf` → torch 2.13.0 + transformers 5.15.0 (fully functional, not self-disabled). `torch-dgl` → torch 2.3.0 + dgl 2.2.1 + torchdata 0.7.1 + numpy<2 + pydantic (isolated fork, doesn't affect the other three groups). Base env (no extras) + all 18 tests confirmed green after every change.

### Phase 3 — Baseline reproduction — DONE (9/9 models: A1, A2, A3, A4, A5, A6, A7, B1, B2)
Full write-up: [`paper/phase3_baseline_reproduction.md`](./paper/phase3_baseline_reproduction.md) — updated per-model as each one is integrated; this section is a summary only.

**Execution strategy decided with the user:** training runs are **sequential**, never parallel, because this machine has only one GPU (NVIDIA RTX A2000, 8GB — confirmed via `nvidia-smi`; 20 CPU cores also available). Running multiple models on the same GPU simultaneously would contend for the same 8GB VRAM and corrupt the empirical compute-timing measurements Phase 2 flagged as still needed. Non-compute code work (adapters, config wiring) can be drafted in parallel; actual training cannot.

**A1 ProcessTransformer — DONE, pilot for the whole approach.** Vendored the architecture only (`src/models/process_transformer/model.py`, Apache-2.0, license file preserved alongside) — deliberately did **not** reuse the original repo's own `data_processing.py`, since that does its own train/test split and vocab-from-combined-data, which would violate spec §6's "identical splits across every model" requirement. Instead wrote `src/models/process_transformer/adapter.py` converting this project's own Phase-2 split parquet into the model's expected tensors, replicating the original's exact prefix-generation semantics. Config-driven training entrypoint: `experiments/train_process_transformer.py`.

Result: trained end-to-end on Helpdesk. Best validation accuracy 86.9% (paper reports 85.63% — close), but test accuracy only 72.9% micro / 63.2% macro-across-k — a real, disclosed gap, most likely from (a) training instability at the paper's stated lr=0.01 (visible loss spikes/recoveries during training) and/or (b) genuine temporal distribution shift under this project's strict chronological split (test is a truly later time period than train/val, unlike a random split which would leak future patterns). Not further hyperparameter-tuned — the pilot's actual goal (validate the full data→train→checkpoint→metrics→provenance pipeline) was met. **Notable independent validation**: reading the actual ProcessTransformer paper (fetched and read directly, not from memory) showed its Table 1 Helpdesk statistics (4,580 cases, 21,348 events, 14 activities, avg length 4.66) match this project's own Phase 2 pipeline output exactly — strong evidence the Phase 2 preprocessing is correct. Confirmed the vendored model's `prefix_representation` (GlobalAveragePooling1D) layer is retrievable by name post-training, ready for Phase 4's z_t extraction without touching the model file again.

**A2 Camargo GenerativeLSTM — DONE.** The original architecture is far more involved than A1's (3 input branches, pretrained/frozen embeddings, role clustering, a 2000-model hyperparameter search across 3 architecture variants). Scoped down consistently with A1's precedent: implemented **only the activity branch** of `model_shared_cat` (embed → LSTM → BatchNorm → LSTM(`prefix_representation`) → Dense-softmax), trainable (not pretrained/frozen) embeddings, no role branch (this project's schema doesn't carry resource data — keeping every model's input data identical, spec §6), fixed hyperparameters taken directly from the repo's own `dg_training.py` defaults (not guessed). The "insecure HTTP dependency" flagged in Phase 1 turned out to be moot: the repo vendors its own `support_modules/` locally, which is what actually gets imported — no external install ever needed.

Refactored the prefix-generation logic shared between A1 and A2 into `src/data/prefixes.py` (verified via regression check that A1's numbers didn't change).

Result: trained on Helpdesk, same split as A1 (confirmed identical `dataset_split_hash`). Best validation accuracy 81.8% (smooth, stable training — unlike A1's), but test accuracy only 63.5% micro / 59.1% macro-across-k. Two real comparison points found (fetched and read the actual Camargo BPM 2019 paper, not from memory): the paper's own Table 4 reports 78.9% on Helpdesk; a third-party re-implementation cited by ProcessTransformer's paper reports 76.51%.

**Notable cross-model finding:** the same validation-vs-test gap that appeared for A1 (Transformer, unstable training) reappears for A2 (LSTM, smooth training) — on the identical data split. Since two architecturally different models with very different training dynamics show the same pattern, this is stronger evidence for **genuine temporal distribution shift** under this project's strict chronological split than for an A1-specific optimization artifact. Both models' validation accuracy also meets or exceeds the original papers' own reported numbers — suggesting the stricter split is surfacing a real generalization gap that looser/ambiguous split methodologies in the original papers may not have exposed. This is being watched as a potential genuine methodological finding, not just a reproduction caveat (see Open questions).

**A4 SuTraN — DONE, first full-suffix model.** Original architecture is a multi-task encoder-decoder (activity + timestamp suffix + remaining runtime). Scoped to **activity-only** — stricter than the repo's own "NDA" variant, since NDA's timestamp *inputs* at inference are themselves autoregressively generated from the model's own timestamp *predictions*; dropping the prediction head while keeping the inputs would be architecturally broken. Every model in the roster now predicts activities only, a deliberately homogeneous target family, not a per-model cut. Core Transformer blocks (attention, FFN, encoder/decoder layers) vendored unmodified from `SuTraN/layers.py`; hyperparameters (`d_model=32`, 4 encoder + 4 decoder layers, 8 heads, `d_ff=128`, `AdamW(lr=0.0002)`, `ExponentialLR(gamma=0.96)`) taken directly from the repo's own `TRAIN_EVAL_SUTRAN_NDA.py`.

A synthetic-tensor smoke test (now standard practice before any real training run) caught a genuine architectural bug before it wasted training time: cross-attention's mask-broadcasting logic assumes decoder query length always equals encoder key length, which breaks during naive incremental autoregressive decoding. Fixed by matching the original repo's own approach — decode with a fixed full-window decoder input at every step (future positions padded, read out via the causal mask), not a dynamically-growing sequence. A related fix: prefix and suffix tensors must share one `window_size` (not independent max lengths), matching the original repo's own convention, for the same mask-broadcast reason.

Result: trained on Helpdesk, same split as A1/A2 (confirmed identical `dataset_split_hash`). **No published-number comparison exists for this pair** — SuTraN's own paper never evaluated Helpdesk (it used BPIC17/BPIC17-DR/BPIC19/BAC) — disclosed as a limitation rather than hidden. Validation-set mean normalized Damerau-Levenshtein similarity (autoregressive generation): **0.924**. Test-set: **0.816**.

**The val/test gap is now confirmed a THIRD time**, across a genuinely different objective (full-suffix generation vs. next-activity classification) and a genuinely different metric (DL-similarity vs. accuracy): A1 86.9%→72.9%, A2 81.8%→63.5%, A4 92.4%→81.6%. Three architectures, two objectives, two metric families, same dataset/split, same qualitative pattern. This has crossed from "worth watching" to "should be its own dedicated analysis" — see the updated open question.

New reusable infrastructure built along the way: `src/data/prefixes.py` gained `make_suffix_prefixes` (shared by A4/A5/B2, the same "one definition per objective type" pattern as A1/A2's next-activity prefixes), and `src/evaluation/suffix_metrics.py` implements normalized Damerau-Levenshtein similarity (unit-tested against hand-worked cases) — this is also the metric spec §8.8 calls for in the Phase 8 future-equivalence study, so it's already available when that phase needs it.

**A5 CRTP-LSTM — DONE.** No autoregressive decoding needed at all (unlike A4) — the paper's own "direct" mechanism: prefix fed as a **left-padded** sequence (opposite of A1/A2/A4's right-padding) to a bidirectional LSTM, so every output position sees the whole prefix via the backward pass regardless of prefix length, letting the model regress onto the entire suffix in one forward pass at both train and eval time. Fastest integration so far — reused A4's vocabulary/suffix-target encoding directly via import (`models.sutran.adapter`), adding only left-padded prefix encoding. Hyperparameters (`d_model=80`, `NAdam(lr=0.002)`, `ReduceLROnPlateau(patience=16)`, early-stop patience 24) taken directly from the repo's own `TRAIN_EVAL_CRTP_LSTM_ND.py`. Same activity-only scope decision as A4.

Result: same split as A1/A2/A4 (confirmed identical `dataset_split_hash`), converged fast (best checkpoint ~epoch 1, early-stopped at 25). No published-number comparison (same reason as A4). **0.917 validation / 0.816 test** mean normalized DL-similarity — remarkably close to A4's 0.924/0.816 despite a completely different architecture (direct BiLSTM vs. autoregressive Transformer decoder). **The val/test gap is now confirmed a FOURTH time** (A1 86.9%→72.9%, A2 81.8%→63.5%, A4 0.924→0.816, A5 0.917→0.816) — 4 architectures, 2 objectives, 2 metric families, one dataset/split, one consistent pattern. This is now a strong, well-replicated finding.

**A3 RLHGNN — DONE, first heterogeneous-GNN model, most up-front reading of any integration so far.** Read `main.py`, `build_graph.py`, `Train.py`, `model/model.py`, `MyDataset.py`, `data_process.py`, and `ProcessEventlog_one_graph.py` in full, plus fetched and read the actual arXiv PDF (2507.02690, not just the abstract), before writing any code — needed to resolve an ambiguity the repo's own CLI defaults left open (`Train.py` argparse default `num-epochs=50` vs. the paper's stated "100 epochs, early stopping after 10"; used the paper's number) and to confirm exactly what the RL/DQN structure-selection stage (`env_train.py`/`final_policy.py`) actually does before deciding to drop it.

**Scope decisions:** (1) skip RL/DQN graph-structure selection entirely, always use the paper's "Comprehensive" fixed graph (forward + backward + repeat_next edges, `action=3`/`build_Bidirect_complex_graph`) — justified by the paper's own ablation (Table V) showing this is the second-best fixed structure, within ~1 GMean point of the full RL policy, while being the richest of the four and keeping the paper's actual technical contribution (relation-specific GraphSAGE aggregation: LSTM for forward/backward, mean for repeat_next); (2) activity-only, same project-wide rule as A1/A2/A4/A5 — the original also embeds resource + two engineered/discretized timestamp features, all dropped. Unlike every other model in the roster, RLHGNN's graphs are **not padded to a fixed window** — one DGL heterograph per prefix, exactly as many nodes as the prefix is long, matching the original's own design; graph construction (including the `repeat_next` edge logic) was ported by hand from `build_graph.py` and verified against a hand-worked example before training. Architecture is a from-scratch reimplementation against the paper's equations and the repo's own `model/model.py` (**the repo has no LICENSE file at all** — confirmed again at integration time, still an open item). Also found and documented a paper/code discrepancy: the paper's prose describes the readout as focusing on "the current activity position" (implying the last node), but the actual code does `dgl.max_nodes(hg, 'h')` (max over every node) — replicated the code's real behavior, since that's what produced the paper's reported numbers.

Result: same split as A1/A2/A4/A5 (confirmed identical `dataset_split_hash`). Training converged fast and stably (best checkpoint at epoch 2, early-stopped at epoch 12 of a 100-epoch budget). **No published-number comparison exists for this pair** (RLHGNN's paper evaluates six BPI12/13/2020 logs, never Helpdesk) — disclosed, not hidden. **Best validation accuracy 86.4%**, but **test accuracy only 72.1% micro / 54.6% macro-across-k** — landing almost exactly on top of A1's 86.9%/72.9% despite a completely unrelated architecture family (heterogeneous GNN vs. Transformer). Per-prefix-length metrics show a sharp accuracy dip at k=1 not seen this sharply in the other models — plausibly an artifact of RLHGNN's very short (1-2 node) prefix graphs giving its GraphSAGE aggregators little structure to exploit, worth a closer look during Phase 5.

**The val/test gap is now confirmed a FIFTH time**: A1 86.9%→72.9%, A2 81.8%→63.5%, A4 0.924→0.816, A5 0.917→0.816, A3 86.4%→72.1% — five models, five architectures, two objectives, two metric families, one dataset/split, one relentlessly consistent pattern. A3 landing almost exactly on A1's numbers despite a totally unrelated architecture is about as strong as single-dataset evidence for genuine temporal distribution shift can get.

z_t extraction: `model.encode(hg)` returns the graph-level max-pooled node embedding (batch, 128), confirmed `(3, 128)` against the trained checkpoint — architecturally distinct from every other model in the roster (the only one that pools over the *whole* prefix graph symmetrically rather than reading out a designated last-position hidden state), flagged for the Phase 5 write-up.

**A6 LUPIN — DONE, first text-narrative/pretrained-LLM model.** Architecturally unlike anything else in the roster: encodes the entire running prefix as a natural-language "story" (one templated sentence per event, activity-only per this project's scope rule — a larger proportional cut than for any other model, since LUPIN's own contribution is precisely the rich attribute-to-text encoding being dropped), tokenizes with the pretrained `prajjwal1/bert-medium` checkpoint's own WordPiece tokenizer, fine-tunes the BERT encoder, and attaches one independent linear classification head per suffix position on the pooled `[CLS]` output — direct/non-autoregressive, same family as A5 CRTP-LSTM, but via K heads on one pooled vector rather than a recurrent per-timestep readout. Two vocabularies in play (BERT's own subword input vocab vs. this project's closed TRAIN-only output class vocab, reused directly from A4's `Vocab`) — a genuine architectural difference, not a bug, unique to this model.

**License:** LUPIN's source (`vinspdb/LUPIN`) is **CC BY-NC-SA 4.0** (non-commercial, share-alike) — `src/models/lupin/` kept isolated per this project's license-isolation policy (same handling as A7 MLMME's planned GPL-3.0 isolation).

A real old-checkpoint/new-library compatibility bug was caught by the now-standard pre-training smoke test: `prajjwal1/bert-medium` predates the modern `Auto*` registry convention, so `AutoModel`/`AutoTokenizer.from_pretrained(...)` both fail under `transformers` 5.15.0 — worked around with concrete `BertModel`/`BertTokenizerFast` classes. Compute-budget deviations from the repo (`batch_size=32` not 8, `max_token_length` capped at 48 not 512, `epochs` capped at 15 not 50) are measured, not guessed: at the repo's own settings one epoch took ~59 min on this project's CPU-only hardware; the tuned settings bring this to ~14 min/epoch. Also did **not** replicate a real checkpoint-selection bug found in the original repo's own training script (`best_model = model` keeps a live reference instead of a snapshot, so it silently saves the last epoch's weights regardless of which epoch actually had the best validation loss).

Result: same split as A1–A5 (confirmed identical `dataset_split_hash`). Ran the full 15-epoch budget (no early stop). **Val 0.926 / test 0.829** mean normalized DL-similarity — **the val/test gap confirmed a SIXTH time** (A1 86.9%→72.9%, A2 81.8%→63.5%, A3 86.4%→72.1%, A4 0.924→0.816, A5 0.917→0.816, A6 0.926→0.829), but notably the *smallest* gap of the four full-suffix models (0.097 vs. A4's 0.108/A5's 0.101) — plausibly because a fine-tuned pretrained-LLM encoder generalizes more robustly across the temporal split than the from-scratch architectures, worth a closer look in Phase 5. No published-number comparison available: LUPIN's paper does evaluate Helpdesk, but the paper itself is paywalled with no arXiv/author copy found, so no number could be independently extracted (disclosed limitation, unlike A4/A5 where the comparison simply doesn't exist).

z_t extraction: `model.encode(input_ids, attention_mask)` returns BERT's pooled `[CLS]` vector, already one-per-prefix (no length-indexed readout needed) — architecturally closest to A3 RLHGNN's single pooled-per-prefix vector.

Full write-up: `paper/phase3_baseline_reproduction.md`'s A6 section.

**A7 MLMME — DONE, the last Family A model, first (and only) adversarially-trained (GAN) one.** Fetched and read the actual SDM 2021 paper in full plus cloned and read the actual repo (`network.py`/`main.py`/`preparation.py`/`suffix.py`) before writing any code, since the exact GAN training mechanism was a genuine architectural question, not a footnote. Resolved: the generator (encoder+decoder, both plain 5-layer/200-hidden unidirectional LSTMs over one-hot event vectors, no embedding layer) and discriminator (same LSTM size + one Linear(200,1) per-timestep realism head) train **jointly from initialization** every mini-batch — no separate pretrain-then-adversarial-fine-tune phase, and no REINFORCE/policy-gradient: discrete sequences are handled via **Gumbel-softmax continuous relaxation** (label-smoothed+near-discrete for the "real" side, exponentially-annealed temperature for the generator's own "fake" side), with the generator update minimizing the sum of an adversarial "fool the discriminator" loss and the standard supervised cross-entropy loss (exactly the paper's Algorithm 1). Found and resolved two real paper/code discrepancies: (1) the paper's Eq. 4.1 states Softmax for the activity head, but the actual code applies an indiscriminate ReLU before cross-entropy — replicated the code's real behavior, per this project's established practice; (2) the reference repo's own generator-update step reuses an already-`.detach()`-ed fake-sequence tensor, which — combined with this project's activity-only scope removing the one output column (duration-time) that let gradient leak through in the original two-headed design — would make the adversarial loss a complete no-gradient no-op for the generator if replicated verbatim; this project deliberately does **not** replicate that specific `.detach()`, restoring genuine gradient flow from the discriminator into the generator's activity-prediction weights while still correctly detaching for the discriminator's own update. A synthetic-tensor smoke test explicitly asserted both halves of this (zero generator gradient after the discriminator-only update, nonzero after the generator's own adversarial update) before any real training was attempted — caught nothing further wrong, confirming the fix works as intended.

**A real environment constraint discovered during A7, not encountered by any earlier model**: this project's execution environment kills long-running background training processes at approximately the 60-minute mark regardless of health. A first attempt at a measured, disclosed 40-epoch budget (down from the paper's own impractical 500 epochs — ~135s/epoch measured, ~18.75 hours at 500) was killed mid-run at ~60 minutes while still actively improving its checkpoint, not crashing on its own. A first, provisional result was reported at a 15-epoch cap (~34 minutes) — the same cap already used for A6 LUPIN's comparably-expensive full-suffix model.

**Superseded 2026-08-17**: rather than accept the 15-epoch cap as final, checkpoint/resume support was added to `experiments/train_mlmme.py` (full training state — both models, both optimizers, loss/timing history, RNG states — snapshotted every epoch to `resume_state.pt`, so re-running the same command resumes with bit-for-bit RNG continuity instead of restarting). This makes the full `epochs=500`/`patience=30` budget reachable via ~20 chunked ~25-epoch invocations (~57 min/chunk, under the 60-minute limit), run by the user directly across several separate sessions. Training converged naturally this time — `patience=30` triggered at epoch 51, the paper's own early-stopping criterion, not an artificial cap. **Final result: Val 0.9323 / test 0.8156** mean normalized DL-similarity (up from the provisional run's 0.9200/0.8034 — confirming the 15-epoch result genuinely was undertrained in absolute terms). **Unlike A4/A5/A6, a genuine published-number comparison exists**: MLMME's own paper reports 0.8411 average SDL on Helpdesk (its own, differently-derived 7:1:2 split) — this project's 0.8156 test result now lands closer to that published number, with the residual gap more plausibly attributable to (a) this project's activity-only scope dropping the duration-time joint-training signal entirely, and (b) the stricter chronological split, rather than undertraining.

**The val/test gap confirmed a SEVENTH time, and no longer budget-confounded** (A1 86.9%→72.9%, A2 81.8%→63.5%, A3 86.4%→72.1%, A4 0.924→0.816, A5 0.917→0.816, A6 0.926→0.829, A7 0.9323→0.8156) — seven architectures, one relentlessly consistent qualitative pattern. A7's gap (0.1167) remains the **largest** of the five full-suffix DL-similarity models (A4 0.108, A5 0.101, A6 0.097, A7 0.117, B2 0.0907 — B2 trained later). Critically, going from 15→51 epochs moved val and test by almost exactly the same amount (+0.0123/+0.0122), leaving the gap itself **unchanged** (0.1166→0.1167) on a run that converged under the paper's own stopping criterion — decisive evidence A7's larger gap is a genuine property of this model/objective, not an artifact of undertraining as originally hedged.

z_t extraction: `model.encode(prefix_onehot, prefix_lengths)` returns the encoder's final top-layer hidden state (batch, hidden_size=200) — a classic seq2seq bottleneck vector, architecturally the roster's cleanest "one vector per prefix" case (closest in spirit to A3/A6's single pooled-per-prefix vectors, via a seq2seq mechanism rather than graph/attention pooling).

**License:** MLMME's source (`farbodtaymouri/MLMME`) is **GPL-3.0** — `src/models/mlmme/` kept isolated per this project's license-isolation policy (same handling as A6 LUPIN's CC BY-NC-SA 4.0).

Full write-up: `paper/phase3_baseline_reproduction.md`'s A7 section.

**B1 controlled Transformer (next-event) — DONE, first Family B model.** Unlike every Family A model, not adapted from any published repo: this project's own from-scratch, controlled architecture (spec §5 — same encoder/embedding-dim/training-budget across objectives, only the objective varied), built on `torch.nn.TransformerEncoder` rather than a hand-rolled or paper-specific implementation, and deliberately not reusing A4 SuTraN's vendored `layers.py` (same textbook attention math, but keeping Family B's provenance unambiguous). Bidirectional self-attention over the observed prefix only (no causal mask — same as A1/A4's own prefix encoders, no future leakage since the prefix is all the model ever sees), classification head on the last non-padded position's hidden state (z_t). Trained on the same shared next-event prefix definition (`data.prefixes.make_next_activity_prefixes`) as A1/A2. `configs/models/controlled_transformer_next.yaml` explicitly documents that its architecture/budget hyperparameters must be copied verbatim into B2's config later, for a genuinely controlled objective-only comparison.

Result: same split as A1–A7 (confirmed identical `dataset_split_hash`), early-stopped at epoch 21/100 (patience 15, best checkpoint at epoch 6). **Best validation accuracy 0.8168**, but **test accuracy only 62.9% micro / 50.6% macro-across-k** — no published-number comparison exists (never evaluated anywhere else), but the val/test gap itself is the finding. Per-prefix-length metrics show a sharp k=1 dip (42.7% vs. ~85%/50% at k=0/k=2) that echoes A3 RLHGNN's own k=1 dip in a second, architecturally unrelated model — now a real hint the dip is data/task-driven rather than an architecture artifact.

**The val/test gap is now confirmed an EIGHTH time, and for the first time on a model with zero external paper's hyperparameters or training recipe involved**: A1 86.9%→72.9%, A2 81.8%→63.5%, A3 86.4%→72.1%, A4 0.924→0.816, A5 0.917→0.816, A6 0.926→0.829, A7 0.9323→0.8156 (after its later full-budget re-run), B1 0.8168→0.6286 micro/0.5059 macro. That a model trained entirely under this project's own control shows the identical qualitative pattern as every vendored Family A model is the strongest evidence yet that this reflects the strict chronological split itself, not an artifact of reproducing any particular paper's methodology.

**B2 controlled Transformer (full-suffix) — DONE, completes Phase 3.** Reuses `ControlledTransformerEncoder` unchanged from B1 (same class, same weights-shape, same computation), adds a `torch.nn.TransformerDecoder`-based suffix decoder (shared activity embedding with the encoder, same design choice as A4 SuTraN's own encoder-decoder but built on the standard library). `configs/models/controlled_transformer_suffix.yaml` copies B1's architecture/budget hyperparameters verbatim, as promised. A genuine simplification over A4/A5: `torch.nn.MultiheadAttention`'s own masks don't couple decoder query length to encoder key length (unlike SuTraN's hand-rolled attention), so prefix/suffix windows are independent and `generate()` grows the decoder input one token at a time rather than needing A4's fixed-full-window workaround.

Result: same split as A1–B1 (confirmed identical `dataset_split_hash` across all 9 models). Early-stopped at epoch 23/100 (patience 15, best checkpoint at epoch 8), no compute-budget cut needed (unlike A6/A7). **Val 0.9263 / test 0.8356** mean normalized DL-similarity — no published-number comparison (this project's own architecture, never evaluated on Helpdesk elsewhere). Per-prefix-length metrics show a smooth, monotonically-easier-with-longer-prefix pattern, **not** the sharp k=1 dip seen in B1/A3's next-event curves — a useful refinement: that dip looks specific to the next-event objective, not a general k=1 property.

**The val/test gap is now confirmed a NINTH time**: A1 86.9%→72.9%, A2 81.8%→63.5%, A3 86.4%→72.1%, A4 0.924→0.816, A5 0.917→0.816, A6 0.926→0.829, A7 0.9323→0.8156 (full-budget re-run), B1 0.8168→0.6286/0.5059, B2 0.9263→0.8356 — all 9 roster models, both families, both objectives. **B2's gap (0.0907) is the smallest of all five full-suffix models** (A4: 0.108, A5: 0.101, A6: 0.097, A7: 0.117, B2: 0.0907), achieved with a full uncapped budget — matching A7 in that neither model's gap is attributable to a compute-driven cut anymore (A7's own later full-budget re-run held its larger gap steady at natural convergence). A second, architecturally unrelated case (alongside A6's fine-tuned-pretrained-encoder finding) of comparatively strong generalization — worth investigating with Phase 5's geometry metrics rather than guessing from predictive metrics alone.

**Phase 3 is now fully complete: all 9 roster configurations trained end-to-end on Helpdesk**, sharing one identical dataset split hash, each with a documented z_t extraction point and a full provenance manifest.

### Phase 4 — Representation extraction — DONE (9/9 models)
Full write-up: [`paper/phase4_representation_extraction.md`](./paper/phase4_representation_extraction.md).

Built `src/representations/{common_fields.py, cache.py}` (model-agnostic per-prefix fields; checkpoint/split/code provenance hashing and staleness detection) plus one `experiments/extract_<model>.py` per roster model — same "reload from manifest, no retrain" pattern already established by `scripts/eval_sutran_val_dl.py`. Each model's own z_t extraction point (documented per-model in `paper/phase3_baseline_reproduction.md` during Phase 3) was wired up as-is; no model code was modified except where a new functional-model wrapper was needed to expose an already-named intermediate layer (A1/A2, TensorFlow).

**Storage:** `results/<dataset>/<model>/embeddings_test.parquet` + `embeddings_manifest.json`, one row per (case_id, k) prefix (long/tidy format — `Z_σ` for a trace is `group by case_id, sort by k`, not a separate nested-array file). Columns: model-agnostic (`case_id`, `k`, `prefix_length`, `case_length`, `true_event`, `remaining_time_seconds`, `outcome`) plus model-specific (`z`, `predicted_event`, `event_probs`, `pred_confidence`, `entropy`, `correct`, and — full-suffix models only — `predicted_suffix`/`true_suffix`/`suffix_dl_similarity`).

**Two deliberate design decisions, both disclosed rather than silently picked:**
1. **"Prediction probabilities"/entropy** are defined uniformly as the softmax distribution over the *immediate next event* for all 9 models, including the 5 full-suffix ones (the first generated/predicted suffix position's distribution) — none of Phase 3's training scripts ever kept softmax probabilities anywhere (all discarded them after `argmax`), so this is new code everywhere, not a reuse of something already computed.
2. **`outcome` is null for every row.** This project's schema has no outcome label at all, and Phase 1 already dropped outcome-prediction as a training objective roster-wide. Explicitly asked whether to define a post-hoc proxy label now vs. defer — decided to leave it null and defer to whichever later phase (6/8/9) actually needs one, rather than guess a definition under Phase 4's narrower scope.

**A real, pre-existing bug found and fixed**, unrelated to anything this phase added: `experiments/train_process_transformer.py` called `build_vocab(parts["train"])` (the raw split), but the A2 integration commit (`09ef0f4`) had refactored `build_vocab` to expect a prefix table instead, without updating this one call site — a regression that would crash any future re-run of A1's training script, discovered only because Phase 4's own extraction script hit the identical bug first. A1's actual checkpoint predates that commit by 3 hours and is unaffected; verified the two vocabulary-construction methods are equivalent for the real Helpdesk train split (identical 12-activity set/order) before fixing both the training script and the extraction script.

**Validation:** every one of the 9 models' per-instance predictions, re-derived from its checkpoint by the extraction script, exactly reproduces (to displayed precision) that model's own Phase 3 test-set metric (accuracy for A1/A2/A3/B1, mean DL-similarity for A4/A5/A6/A7/B2) — strong end-to-end evidence the whole pipeline (vocab/split reconstruction, z_t hook, prediction/probability computation) is correct across all 4 frameworks in the roster (TensorFlow/Keras, PyTorch, PyTorch+DGL, PyTorch+HuggingFace). Cache-versioning was verified functionally, not just unit-tested: re-running an extraction script with nothing changed prints "cache up to date, skipping" instead of recomputing.

z_t dimensionality varies widely across the roster (36/100/128/32/80/512/200/64/64 for A1–B2) — largest by far is A6 LUPIN's 512 (`prajjwal1/bert-medium`'s hidden size), flagged for Phase 5 to normalize for when comparing intrinsic dimensionality *within* each model's own space, never raw magnitude across models (spec §14).

---

## Decision log

Append one entry per decision, dated, with a one-line rationale. Never edit past entries — supersede them with a new dated entry if a decision changes.

- **2026-08-14** — Created PLAN.md/STATUS.md/CLAUDE.md tracking system and empty reproducibility directory skeleton per spec §21. No implementation work started yet.
- **2026-08-14** — Chose `uv` as the Python dependency manager (user confirmed over pip+pip-tools and poetry). Installed via `pip install --user uv` since it wasn't preinstalled.
- **2026-08-14** — Completed Phase 0: pyproject.toml (`requires-python>=3.11,<3.14`, non-package project via `[tool.uv] package = false`), uv.lock, .gitignore, pytest wiring with a passing placeholder test, configs/README.md, and README.md. Deferred the experiment-tracking-format decision to Phase 3 (not needed until real training runs exist).
- **2026-08-14** — Completed Phase 1 literature/repository audit via three parallel research passes + hands-on clone verification of the two Family A picks. Finalized an initial 6-model roster (A1 ProcessTransformer, A2 SuTraN, A3 CRTP-LSTM, B1/B2/B3 controlled Transformer variants) — see `paper/related_work_model_audit.md` for full reasoning and rejected alternatives.
- **2026-08-14** — User reviewed the roster and judged one model per Family A category too weak for reviewers. Decided: (a) drop outcome prediction entirely, not just from Family A — no Family B outcome model either, H3 explicitly deferred/untested; (b) promote Camargo et al.'s `GenerativeLSTM` (BPM 2019, peer-reviewed, actively maintained) to a full second Family A next-event model, giving a Transformer-vs-LSTM architectural contrast; (c) run one more targeted research pass for a second full-suffix Family A model with independently-confirmed peer review, rather than default to CRTP-LSTM (whose original paper's venue was never confirmed) — user explicitly requires this choice to be thoroughly justified in the paper itself, not just logged internally.
- **2026-08-14** — Follow-up search resolved CRTP-LSTM's provenance: it is Gunnarsson et al., IEEE Transactions on Services Computing 16(4), 2023 — genuinely peer-reviewed. Also surfaced I3SP (BPM 2025 Workshops/LNBIP 569, independent authors) as an option to address a "SuTraN/CRTP-LSTM are the same lab" critique. User decided to finalize the roster at SuTraN+CRTP-LSTM (both now confirmed peer-reviewed, independence bar met) rather than add I3SP as a third suffix model — keeps the roster at 6 total, I3SP logged as a deferred fallback. **Phase 1 is now fully closed** with a final 6-model roster: A1 ProcessTransformer, A2 Camargo GenerativeLSTM, A3 SuTraN, A4 CRTP-LSTM, B1/B2 controlled Transformer (next-event/suffix), no outcome objective.
- **2026-08-14** — Chose the TF/PyTorch coexistence strategy for Phase 2/3: two separate `pyproject.toml` optional-dependency groups (`tf`, `torch`) in one project, rather than fully isolated per-model environments — keeps one lockfile/one repo while still letting each framework be installed independently.
- **2026-08-14** — Chose the 5-dataset roster (BPIC12, BPIC17, BPIC19, Sepsis, Helpdesk) for Phase 2, verified via a research fork that all 5 have live 4TU.ResearchData download links and that the roster isn't cherry-picked for structural diversity. Not yet written up as a committed dataset-selection doc.
- **2026-08-14** — User flagged that GNN-based next-event models were never searched for in Phase 1, and questioned whether the ML4PM paper should narrow to next-event-only (broad architecture sweep) with full-suffix+outcome deferred to a journal extension. Rather than decide on assumptions, ran a GNN-specific audit first: found ~10 GNN-for-PPM papers but only 2 with any repo, both unpeer-reviewed 2025 preprints — confirmed the category is too thin to justify re-scoping, and the user kept the paper's cross-objective design.
- **2026-08-14** — User then asked for a full systematic literature review (not sequential ad-hoc audits) to avoid missing other categories. Scoped to next-event + full-suffix only (outcome stays dropped), 2021–2026. Ran two more systematic passes; user explicitly relaxed the adoption bar to "peer-reviewed or arXiv preprint + usable repo" and redefined the goal as 3–4 architecturally distinct models per category for experimentation, paper-inclusion decided later.
- **2026-08-14** — Systematic full-suffix pass found LUPIN (ICPM 2024, BERT-based) and MLMME (SDM 2021, GAN-based) as 2 more reproducible candidates. User decided: adopt both, bringing full-suffix to 4 models (SuTraN, CRTP-LSTM, LUPIN, MLMME) spanning 4 architectural paradigms.
- **2026-08-14** — Systematic next-event pass found TGN-AST (BPM 2025 main track) as a candidate; hands-on source inspection revealed it's actually a TF/Keras Transformer variant (graph component is a decoupled pretraining step, not an end-to-end GNN) — rejected despite its strong peer-review venue. User decided to adopt RLHGNN instead (the one GNN audit candidate with real, working single-case code) despite RLHGNN having no LICENSE file at all — a disclosed risk, not a hidden one. Final next-event roster: 3 models (ProcessTransformer, GenerativeLSTM, RLHGNN).
- **2026-08-14** — **Phase 1 is now fully and finally closed** at a 9-configuration roster (A1–A7 Family A + B1/B2 Family B), well beyond the spec's stated 3–6 by explicit user direction. `pyproject.toml` extended with `torch-hf` (transformers, for LUPIN) and `torch-dgl` (dgl, for RLHGNN) optional-dependency groups alongside the existing `tf`/`torch`; `uv lock` re-run and confirmed resolving cleanly.
- **2026-08-15** — Downloaded and processed all 5 datasets; wrote `paper/dataset_selection.md` with real computed statistics (not literature figures). Built the shared preprocessing pipeline (`src/data/` package: schema, loaders, splits, stats) with 18 passing unit tests against synthetic data.
- **2026-08-15** — Smoke-testing the 4 dependency groups found TensorFlow 2.19.1 has no CPython 3.13 wheels. **Switched the whole project from Python 3.13.0 to 3.12.10** rather than fragmenting per-framework Python versions — every other framework in the roster supports 3.12.
- **2026-08-15** — Same smoke test found a chain of DGL/torch/torchdata/numpy/pydantic compatibility issues for the `torch-dgl` group (RLHGNN), and a uv resolver behavior (universal lockfile forcing one extra's pin onto another) that broke `torch-hf` (LUPIN/transformers). Fixed all of it: pinned `torch==2.3.0`/`dgl==2.2.1`/`torchdata==0.7.1`/`numpy<2`/`pydantic` for `torch-dgl`; added `[tool.uv] conflicts` so extras resolve independently; pinned `torch>=2.5` explicitly for `torch-hf` (transformers' real minimum is a runtime-only check invisible to the resolver). All 4 groups now verified to actually import and run, not just install. **Phase 2 is now fully closed.**
- **2026-08-15** — User asked how Phase 3 training would be executed (sequential vs. parallel) before starting. Checked hardware (`nvidia-smi`): one NVIDIA RTX A2000 8GB GPU, 20 CPU cores. Decided: sequential training only, since a single shared GPU would make parallel runs contend for VRAM and corrupt the still-pending compute-timing measurements; non-compute integration code (adapters, config wiring) can still be drafted in parallel across models.
- **2026-08-15** — Completed A1 ProcessTransformer as the Phase 3 pilot. Deliberately did not reuse the original repo's own data preprocessing/split (would violate spec §6's identical-splits requirement) — wrote a dedicated adapter onto this project's own Phase 2 split instead. Fetched and read the actual paper PDF to get real comparison numbers (85.63% accuracy, 100 epochs, lr=0.01) rather than relying on memory or the repo's non-matching CLI defaults (10 epochs, lr=0.001). Achieved 86.9% best validation accuracy (close to paper) but only 72.9% test accuracy — disclosed as a real, unresolved gap (likely lr=0.01 training instability and/or genuine temporal drift under strict chronological splitting), not hidden or hyperparameter-tuned away, since the pilot's actual goal (validate the full pipeline) was met.
- **2026-08-15** — Completed A2 Camargo GenerativeLSTM. Scoped down to the activity-only path of `model_shared_cat` (no role branch, trainable not pretrained embeddings, fixed not searched hyperparameters), consistent with A1's own scope decision — full multi-task/pretrained-embedding/2000-model-search reproduction judged disproportionate to this project's needs. The Phase 1 "insecure HTTP dependency" finding turned out to be moot: the repo vendors its own `support_modules/` locally, so vendoring the architecture code (as done for A1) never needed that external install. Refactored the shared prefix-generation logic into `src/data/prefixes.py` so A1 and A2 (and future next-event models) use one definition, verified via regression test. Fetched and read the actual Camargo BPM 2019 paper for a real comparison number (78.9% on Helpdesk, Table 4) rather than relying on memory. Result: 81.8% best validation accuracy (smooth, stable training) but only 63.5% test accuracy — the same qualitative val/test gap pattern as A1, despite very different training dynamics (A1 unstable, A2 smooth). Logged as stronger evidence for genuine temporal distribution shift under this project's strict chronological split, not an A1-specific optimization artifact — a candidate for a real, reportable methodological finding if it keeps recurring.
- **2026-08-15** — Completed A4 SuTraN, the first full-suffix model. Scoped to activity-only, stricter than the repo's own "NDA" variant (NDA's timestamp inputs at inference are autoregressively derived from the model's own timestamp predictions, so dropping just the prediction head while keeping those inputs would be architecturally broken) — every roster model now predicts activities only, deliberately homogeneous, not per-model ad-hoc. A pre-training synthetic-tensor smoke test caught a real bug: cross-attention's mask-broadcasting assumes decoder query length equals encoder key length, which breaks under naive incremental autoregressive decoding — fixed by adopting the original repo's own fixed-full-window decoding approach, plus using one shared `window_size` for both prefix and suffix tensors (not independent max lengths) for the same underlying reason. No published-number comparison exists for Helpdesk (SuTraN's paper never evaluated it) — disclosed, not hidden. Result: 0.924 validation / 0.816 test mean normalized DL-similarity (autoregressive generation) — **the val/test gap recurs a third time**, now across a different objective (suffix generation vs. classification) and a different metric (DL-similarity vs. accuracy), crossing from "worth watching" to "should be its own dedicated analysis." Built reusable infrastructure along the way: `data.prefixes.make_suffix_prefixes` (shared suffix-prefix definition for A4/A5/B2) and `src/evaluation/suffix_metrics.py` (normalized Damerau-Levenshtein similarity, unit-tested, also needed later for Phase 8 per spec §8.8). While integrating A4, also found SuTraN's own exact BPIC19 chronological-outlier cutoffs (`start_date="2018-01"`, `end_date="2019-02"`) in its `create_BPIC19_data.py` — resolves this project's own open BPIC19 timestamp-cleaning question, properly attributed, not yet implemented in our own pipeline.
- **2026-08-15** — Completed A5 CRTP-LSTM. No autoregressive decoding needed at all — its "direct" mechanism feeds the prefix as a left-padded sequence (opposite of every other model's right-padding) to a bidirectional LSTM, so the backward pass gives every output position full-prefix context regardless of length, letting one forward pass regress onto the whole suffix. Fastest integration yet: reused A4's vocab/suffix-target encoding by direct import rather than duplicating it. Hyperparameters (`d_model=80`, `NAdam(lr=0.002)`, `ReduceLROnPlateau(patience=16)`) taken directly from the repo's own script. Result: 0.917 validation / 0.816 test DL-similarity, nearly identical to A4 despite a totally different architecture. **Val/test gap confirmed a fourth time** across 4 architectures/2 objectives/2 metric families on the same split — now a strong, well-replicated finding worth designing a dedicated analysis around.
- **2026-08-15** — Completed A3 RLHGNN, the first heterogeneous-GNN model and the most up-front reading any integration has needed (full read of `main.py`, `build_graph.py`, `Train.py`, `model/model.py`, `MyDataset.py`, `data_process.py`, `ProcessEventlog_one_graph.py`, plus the actual arXiv PDF fetched and read to resolve a repo-vs-paper hyperparameter ambiguity — used the paper's "100 epochs, patience 10" over the repo CLI's `num-epochs=50` default). Dropped the RL/DQN graph-structure-selection stage entirely, always using the paper's "Comprehensive" fixed graph (forward+backward+repeat_next edges) — justified via the paper's own ablation table showing this is the second-best fixed structure, within ~1 GMean point of the full RL policy, while preserving the paper's actual technical contribution (relation-specific GraphSAGE aggregation). Applied the project's activity-only rule (dropping resource + 2 engineered timestamp features the original also embeds). Unlike every other roster model, graphs are unpadded (one node per real prefix event) — ported edge-construction logic by hand from `build_graph.py`, verified against a hand-worked example before training. Found and documented a paper/code discrepancy (prose describes last-node readout, code does whole-graph max-pooling) and replicated the code's actual behavior. Repo has no LICENSE file at all, confirmed again (still an open item). Result: 86.4% best validation accuracy, 72.1% test micro accuracy — landing almost exactly on A1's numbers (86.9%/72.9%) despite a completely unrelated architecture. **Val/test gap confirmed a FIFTH time** across 5 architectures/2 objectives/2 metric families on the identical split.
- **2026-08-16** — Completed A6 LUPIN, the first text-narrative/pretrained-LLM model. Scoped its input to activity-only text stories (one templated sentence per event) — the largest proportional cut of any roster model, since LUPIN's actual contribution is the rich multi-attribute text encoding this project must drop for cross-model comparability. Kept `src/models/lupin/` isolated as CC BY-NC-SA 4.0 (non-commercial), same handling planned for A7 MLMME's GPL-3.0. A pre-training smoke test caught a real old-checkpoint/new-`transformers`-version incompatibility (`prajjwal1/bert-medium` predates the `Auto*` registry convention) — worked around with concrete `BertModel`/`BertTokenizerFast` classes. Measured (not guessed) compute-budget deviations: repo's own settings (batch_size=8, MAX_LEN=512) measured ~59 min/epoch on this project's CPU-only hardware, made impractical; tuned to batch_size=32/max_token_length capped at 48 (covers ~92-93% of TRAIN prefixes untruncated) → ~14 min/epoch, epochs capped at 15. Deliberately did not replicate a real checkpoint-selection bug in the original repo's own training script (keeps a live model reference instead of a state_dict snapshot, so it silently saves the last epoch regardless of which had the best validation loss). Result: 0.926 validation / 0.829 test mean normalized DL-similarity, ran the full 15-epoch budget with no early stop. **Val/test gap confirmed a SIXTH time**, but the smallest gap (0.097) of the four full-suffix models so far — plausibly because fine-tuning a pretrained LLM generalizes more robustly across the temporal split than the from-scratch architectures. No published-number comparison possible: LUPIN's ICPM 2024 paper does evaluate Helpdesk, but it's paywalled with no arXiv/author-hosted copy found.
- **2026-08-16** — Completed A7 MLMME, the last Family A model and the roster's only adversarially-trained (GAN) one. Fetched and read the actual SDM 2021 paper plus cloned and hand-read the actual repo code before writing anything, resolving the exact GAN training mechanism as Gumbel-softmax continuous relaxation (not REINFORCE) with generator+discriminator trained jointly from initialization every mini-batch (no separate pretrain-then-adversarial phase) — Algorithm 1's generator objective is the sum of an adversarial loss and the standard supervised cross-entropy loss. Found and disclosed two real paper/code discrepancies (ReLU-before-cross-entropy instead of the paper's stated Softmax; and a reference-repo `.detach()` call that, combined with this project's activity-only scope removing the one column that let gradient leak through in the original two-headed design, would silently zero out the entire adversarial loss's gradient to the generator if replicated verbatim — deliberately not replicated, with a synthetic-tensor smoke test explicitly asserting the fix works before any real training). Discovered a new, general environment constraint along the way: this project's execution environment kills long-running background training processes at ~60 minutes regardless of health (a first 40-epoch attempt was killed mid-run while still healthily improving) — recovered by cutting to a measured, disclosed 15-epoch budget (~34 minutes), the same cap already used for A6. Result: 0.9200 validation / 0.8034 test mean normalized DL-similarity, training fully monotonic with no early stop (matching A6's "ran the full capped budget" outcome). Unlike A4/A5/A6, a genuine published-number comparison exists (paper's own Helpdesk SDL: 0.8411, different split) — this project's 0.8034 lands reasonably close but below it, plausibly because A7 received by far the smallest fraction (3%) of its own paper's recommended training budget of any full-suffix model here. **Val/test gap confirmed a SEVENTH time**, and is the *largest* gap (0.117) of the four full-suffix DL-similarity models — breaking, not continuing, the narrowing trend A4→A5→A6 had shown; read as likely undertraining given A7's much smaller relative budget, not new evidence against A6's pretrained-encoder-generalizes-better hypothesis (confounded, not refuted, by this result).
- **2026-08-16** — Completed B1 controlled Transformer (next-event), the first Family B model. Deliberately built as a from-scratch encoder on `torch.nn.TransformerEncoder` rather than reusing A4 SuTraN's vendored (though textbook-equivalent) attention layers, to keep Family B's provenance unambiguous — no external repo, paper hyperparameters, or training recipe involved anywhere in this model. Encoder is bidirectional over the observed prefix (no causal mask, same rationale as A1/A4), z_t = last non-padded position's hidden state. `configs/models/controlled_transformer_next.yaml` explicitly documents that its hyperparameters must be copied verbatim into B2's config later, so the eventual B1-vs-B2 comparison isolates objective from architecture per spec §5. Result: 0.8168 validation accuracy, early-stopped at epoch 21/100, but only 62.9% micro/50.6% macro-across-k test accuracy. **Val/test gap confirmed an EIGHTH time** — first time on a model with zero external paper involved at all, the strongest evidence yet that the gap reflects this project's strict chronological split rather than an artifact of reproducing any particular paper's methodology. Per-k metrics show a k=1 accuracy dip (42.7%) echoing A3 RLHGNN's own k=1 dip in a second, unrelated architecture — now suspected to be data/task-driven rather than architecture-specific, worth checking directly in Phase 5.
- **2026-08-16** — Completed B2 controlled Transformer (full-suffix), completing Phase 3 (9/9 models). Reused `ControlledTransformerEncoder` unchanged from B1, added a `torch.nn.TransformerDecoder`-based suffix decoder (shared activity embedding, same design choice as A4 SuTraN's own encoder-decoder but library-based rather than hand-rolled). Copied B1's architecture/training-budget hyperparameters verbatim into `configs/models/controlled_transformer_suffix.yaml`, as promised when B1 was built. A genuine simplification over A4/A5's adapter: since `torch.nn.MultiheadAttention`'s masks don't couple decoder query length to encoder key length (unlike SuTraN's hand-rolled attention), B2's prefix/suffix padded windows are independent, and `generate()` grows the decoder input one token at a time rather than needing A4's fixed-full-window workaround. Result: 0.9263 validation / 0.8356 test mean normalized DL-similarity, early-stopped at epoch 23/100 with no compute-budget cut needed. **Val/test gap confirmed a NINTH time**, and B2's gap (0.0907) is the smallest of all five full-suffix models in the roster — notably achieved with a full, uncapped budget, so (unlike A7's *then-provisional* result) it can't be explained away by undertraining; a second, architecturally unrelated case of comparatively strong generalization alongside A6 LUPIN's own finding, worth investigating with Phase 5's geometry metrics. Per-k metrics show a smooth, monotonically-easier-with-longer-prefix curve, not the sharp k=1 dip B1/A3 show on the next-event objective — refines that open question to "next-event-specific," not a general k=1 property. **Phase 3 is now fully complete.**
- **2026-08-17** — User ran A7 MLMME's full `epochs=500`/`patience=30` budget in a separate terminal, using the checkpoint/resume support added to `experiments/train_mlmme.py` during A7's original integration (full training state, including RNG states, snapshotted every epoch to `resume_state.pt`; re-running the same command resumes with bit-for-bit continuity). Chunked across ~20 invocations of ~25 epochs each to stay under this environment's ~60-minute background-process limit (a constraint specific to this project's own tooling, not to a user-run terminal, but the chunking approach was already built and reused regardless). Training converged naturally this time: `patience=30` triggered at epoch 51 — the paper's own early-stopping criterion, not a compute cap. **Result: val 0.9323 / test 0.8156 mean normalized DL-similarity**, improved from the provisional 15-epoch run's 0.9200/0.8034, and closer to the paper's own published 0.8411 SDL. **Critically, the val/test gap itself barely moved** (0.1166→0.1167) despite the large jump in absolute performance — decisive evidence that A7's comparatively large gap (still the largest of all 5 full-suffix models) is a genuine property of this model/objective, not the undertraining artifact the original 15-epoch write-up had hedged it might be. Superseded the provisional numbers throughout STATUS.md and `paper/phase3_baseline_reproduction.md` (per this file's policy, the original 2026-08-16 decision-log entry is left as historical record, not edited).
- **2026-08-17** — Completed Phase 4 (representation extraction) for all 9 models. Key decisions: (a) storage colocated at `results/<dataset>/<model>/embeddings_test.parquet` (not a separate `data/processed/embeddings/` tree, one of PLAN.md's two suggested options) — keeps embeddings next to the checkpoint/manifest they were derived from, following this project's existing per-model results-directory convention; (b) "prediction probabilities"/entropy defined as the immediate-next-event distribution uniformly across next-event and full-suffix models, since none of Phase 3's training scripts retained softmax probabilities anywhere; (c) `outcome` left null for every row (user confirmed, choosing to defer a concrete definition to whichever later phase actually needs one, over inventing a post-hoc proxy label now) — no outcome label exists anywhere in this project's schema, consistent with Phase 1's decision to drop outcome-prediction entirely. Found and fixed a real pre-existing regression in `train_process_transformer.py` (a call site never updated after the A2 commit refactored `build_vocab`'s expected input) — verified it didn't affect A1's already-trained checkpoint (trained 3 hours before that commit) before fixing. All 9 models' extracted predictions exactly reproduce their own Phase 3 test metrics, and the cache-versioning/staleness mechanism was verified to actually skip recomputation when nothing changed, not just unit-tested. Full write-up: `paper/phase4_representation_extraction.md`.

---

## Open questions / not yet decided

- Experiment tracking/logging format (lightweight structured logs under `results/` vs. an external tracker) — deferred until Phase 3 needs to log actual training runs.
- Number of seeds per model/dataset — depends on compute budget; needs the empirical timing pilot (now that a real dataset pipeline exists, this is unblocked) before deciding. More important given 9 models × 5 datasets = up to 45 combinations.
- Exact torch/CUDA target for the `torch` and `torch-hf` groups (currently unpinned beyond a floor; `torch-dgl` is hard-pinned to 2.3.0 for compatibility reasons already explained) — decide once more PyTorch models are integrated (A1 didn't need this, it's TF-only).
- RLHGNN dependency versions are now pinned (`torch-dgl` extra: `torch==2.3.0`, `dgl==2.2.1`, `torchdata==0.7.1`, `numpy<2`, `pydantic`) — resolved during A3's integration, no longer open.
- Current PyTorch installs are CPU-only wheels (confirmed via A4's manifest: `"device": "cpu"`, despite the machine having an NVIDIA RTX A2000 GPU) — training is currently not using the GPU at all for PyTorch models. Fine for Helpdesk-scale runs so far; will matter once BPIC17/BPIC19-scale training starts. Needs a CUDA-enabled torch install decision (see the `torch`/`torch-hf` CUDA-target item above).
- ICPM 2027 ML4PM workshop CFP details (page limit, format, deadline) — checked 2026-08-14, not published yet; conference dates (Feb 8–12, 2027) are confirmed. Re-check closer to Phase 11.
- **RLHGNN's missing license** — confirmed again during A3's integration (no LICENSE file in the repo). `src/models/rlhgnn/{model.py,adapter.py}` are a from-scratch reimplementation against the public paper + code (not a verbatim copy), disclosed in both files' docstrings; still worth deciding whether to email the authors for clarification before relying on it further, and exactly how to phrase the gap in the paper itself.
- **Which of the 9 models actually make it into the ML4PM workshop paper vs. stay as "ran the experiment, held for a journal extension"** — user explicitly deferred this decision ("we'll see later if we integrate them in the paper"). Worth revisiting once Phase 6's descriptive results exist and it's clearer which comparisons are most informative.
- Whether to pursue any of the ~15 no-repo papers found during the systematic review (emailing authors, reimplementing) — logged as a journal-extension-scale task, not blocking the workshop paper.
- BPIC19's timestamp data-quality artifact (1948-01-26 start) — **rule now known, not yet implemented**: SuTraN's own repo uses `start_date="2018-01"`, `end_date="2019-02"` chronological cutoffs for exactly this issue (found in `create_BPIC19_data.py` while integrating A4). Still need to apply this in our own `src/data`/`configs/datasets/bpic19.yaml` before training on BPIC19.
- **The val/test gap now confirmed across ALL NINE models** (A1: 86.9%→72.9% accuracy; A2: 81.8%→63.5% accuracy; A3: 86.4%→72.1% accuracy; A4: 0.924→0.816 DL-similarity; A5: 0.917→0.816 DL-similarity; A6: 0.926→0.829 DL-similarity; A7: 0.9323→0.8156 DL-similarity, full-budget re-run; B1: 0.8168→0.6286 micro/0.5059 macro accuracy; B2: 0.9263→0.8356 DL-similarity), spanning both Family A and Family B, both prediction objectives, and both metric families, all on the identical Helpdesk split. B1/B2 (zero external paper's hyperparameters, split, or training recipe involved at all) are the strongest evidence yet that this is a genuine property of the strict chronological split itself, not an artifact of reproducing any particular paper's methodology. This has crossed the threshold from "worth watching" to "should become its own dedicated analysis" in the paper — worth designing that analysis explicitly once at least one other dataset confirms the pattern isn't Helpdesk-specific. **Full-suffix gap ranking is now**: B2 (0.0907, smallest) < A6 (0.097) < A5 (0.101) < A4 (0.108) < A7 (0.1167, largest). **A7's largest gap is no longer budget-confounded**: its follow-up full run (patience=30 naturally triggered at epoch 51, not a compute cap) moved val and test up by almost exactly the same amount, leaving the gap itself unchanged from the provisional 15-epoch result — decisive evidence this is a genuine property of A7's model/objective, not undertraining. Two of the two smallest-gap models (A6 pretrained-BERT, B2 in-house Transformer with a full uncapped budget) are architecturally unrelated to each other, which argues against either being a fluke of one specific model family — worth investigating directly with Phase 5's geometry metrics (e.g. z_t's local neighborhood structure or intrinsic dimensionality) rather than continuing to guess from predictive metrics alone, and now A7's genuinely-largest gap is worth the same treatment rather than being dismissed as a training artifact.
- **RLHGNN's per-prefix-length metrics show a sharp accuracy dip at k=1** (43.4% vs. ~85% at k=0 and k=2), not seen this sharply in A1/A2/A4/A5/A7. **B1's own per-k metrics show the same dip (42.7%) in a second, architecturally unrelated model** (plain bidirectional Transformer vs. heterogeneous GNN), but **B2's own per-k metrics show no such dip** (smooth, monotonically-easier-with-longer-prefix curve) — this refines the open question: the k=1 dip now looks specific to the **next-event objective** (predicting the 2nd event of a case is genuinely harder, for reasons still unclear), not a general property of prefix length k=1 across every task. Worth checking directly once Phase 5's geometric analysis is running, ideally against z_t's own local structure at k=1 for the affected models.
- **A new environment constraint discovered during A7's integration**: this project's execution environment kills long-running background training processes at approximately the 60-minute mark regardless of process health (confirmed directly — a 40-epoch MLMME attempt was killed mid-run while its checkpoint was still actively improving, not crashing on its own). Not yet an issue for any other model trained so far (all finished within an hour), but a real constraint to plan around for B1/B2 and any future BPIC17/BPIC19-scale training: cap epoch budgets/checkpoint frequently enough to make a restart cheap, rather than assuming an unbounded single background run.

## Blockers

None currently.

## Next steps (pick these up first in the next session)

1. **Start Phase 5 — geometry metric implementation & synthetic validation** (PLAN.md, spec §8/§9/§22): implement the modular geometry-analysis package (trajectory straightness, path length, velocity, curvature/direction-change, distance-to-class-centroids, future-similarity-vs-latent-distance, etc.) and **validate every metric against synthetic latent trajectories before touching any of Phase 4's real cached embeddings** — this is an explicit spec requirement ("Do this before analyzing any real model"), not optional sequencing.
2. Once Phase 5's metrics are validated, the first real analysis to run them against is the 9-for-9 val/test gap pattern and the k=1 next-event dip (see Open questions below) — both are now well-replicated predictive-metric findings waiting on a geometric explanation, not further guessing from accuracy/DL-similarity alone.
3. Implement the BPIC19 cleaning rule now that the exact cutoffs are known (`start_date="2018-01"`, `end_date="2019-02"`, from SuTraN's own preprocessing) — update `configs/datasets/bpic19.yaml` and the loading pipeline before any model is trained on BPIC19.
4. Decide whether to install a CUDA-enabled torch build — current PyTorch training is CPU-only despite the machine having an NVIDIA RTX A2000 GPU. Not urgent for Helpdesk-scale runs, will matter for BPIC17/BPIC19-scale training or extraction.
5. Pin exact dependency versions for plain `torch`/`torch-hf` (still unpinned beyond a floor; `torch-dgl` is already exactly pinned).
6. The val/test gap is now a 9-for-9 pattern across the complete roster (see Open questions) — start sketching what a dedicated analysis of it would look like (e.g. does it hold on other datasets too, or is it Helpdesk-specific?) once a non-Helpdesk dataset is in the mix. B2's smallest-of-all full-suffix gap (with a full uncapped budget), A6's own smallest-among-its-peers gap, and A7's now-confirmed-genuine (not budget-confounded) largest gap are three data points across unrelated architectures worth explaining via Phase 5's geometry metrics rather than guessing further from predictive metrics alone.
7. The k=1 accuracy dip is now refined to look next-event-specific (seen in RLHGNN and B1, absent in B2's full-suffix curve) — worth checking directly once Phase 5's geometry metrics are running, against z_t's own local structure at k=1 for the affected models.
8. `outcome` is null in every Phase 4 embedding cache by deliberate decision (no label exists in this project's schema, and defining one was explicitly deferred rather than guessed) — revisit only if a specific later phase (6/8/9) genuinely needs an outcome axis; don't invent one speculatively.
9. Consider whether Phase 5+ will need train/val-split embeddings too (Phase 4 only extracted test-set embeddings, per spec §7's literal wording and the already-recorded next-steps plan) — re-run `experiments/extract_<model>.py` against the relevant split if a later phase needs it; the extraction scripts and cache-versioning infrastructure generalize directly, nothing new needs to be built.
